# Example environment variables for running the counter against a local dev Vault
# Set to '1' to skip TLS verification (useful for HTTPS with self-signed certs)
VAULT_ADDR=http://127.0.0.1:8200
# Do NOT store real tokens in source control. Copy this file to `.env` and set the token there.
VAULT_TOKEN=
VAULT_SKIP_VERIFY=1
# Or provide a CA cert path
# VAULT_CACERT=/path/to/ca.pem

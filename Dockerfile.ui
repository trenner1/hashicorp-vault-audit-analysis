# Build frontend locally first with: cd frontend && npm run build
# Then build this image with the pre-built dist folder

FROM nginx:alpine
COPY frontend/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
